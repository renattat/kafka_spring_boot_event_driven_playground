spring:
  cloud:
    function:
      definition: producer
    stream:
      poller:
        fixed-delay: 1000
      bindings:
        producer-out-0:
          destination: order-events
      kafka:
        binder:
          brokers:
            - localhost:9092