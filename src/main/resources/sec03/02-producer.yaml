spring:
  cloud:
    function:
      definition: producer
    stream:
      poller:
        fixed-delay: 100 # default value (1000)
      bindings:
        producer-out-0:
          destination: demo-topic
      kafka:
        binder:
          brokers:
            - localhost:9092